# Copy to .env.local for local dev
# Set VITE_API_URL to your deployed Render backend URL for production

# Local dev (default — leave blank to use localhost:8000)
VITE_API_URL=

# Production example (replace with your actual Render URL after deploying):
# VITE_API_URL=https://fusion-engine-api.onrender.com
